import React from 'react';
import Carousel from "react-bootstrap/Carousel";


const CarouselItem = ({location}) => {
  const {img, name, body} = location;
  console.log(img);
    return (
      <div></div>
    );
};

export default CarouselItem;